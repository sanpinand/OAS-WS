import { Express } from "express";
import { Middleware, OpenAPIV3Doc } from "../../typings";
export declare class OASBase implements Middleware {
    #private;
    constructor(oasFile: OpenAPIV3Doc, middleware: Function);
    register(app: Express, path: string): void;
    initialize(oasFile: OpenAPIV3Doc, _config: object): OASBase;
    getMiddleware(): Function;
}
